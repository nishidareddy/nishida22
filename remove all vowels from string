#include<stdio.h>
#include<string.h>
int isvowel(char);

void main()
{
 

  char str[100],new[100];
  int i,j=0;

  printf("Enter a String To Remove Vowels \n\n");
  gets(str);

  for(i = 0; str[i] != '\0'; i++)
  {
   if(isvowel(str[i]) == 1)
   {
    new[j] = str[i];
    j++;
   }
  }
